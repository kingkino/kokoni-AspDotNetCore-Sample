@{
    // Controller名を取得してレイアウトの適応判断
    var controller = this.ViewContext.RouteData.Values["controller"].ToString();

    // ログイン画面は共通レイアウトを利用しない
    Layout = (controller.ToLower() == "login")? null:"_Layout";

}